package constants

const (
	TERRAFORM_DEFAULT_ALLOWED_IP = "0.0.0.0"
	TERRAFORM_LOCK               = ".terraform.lock.hcl"
	TERRAFORM_STATE              = "terraform.tfstate"
	TERRAFORM_BACKUP             = "terraform.tfstate.backup"
)
